When creating markdown documents always create them in `.claude/docs/`
Never use mock or fake data anywhere in the codebase. If data is needed for tests, retrieve real data, save it, and use it in tests. Always acknowledge when data may be inaccurate or out of date.
